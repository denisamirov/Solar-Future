export const BASE_URL = 'http://localhost:4000';

export const endpoints = {
  switch: `${BASE_URL}/switch`,
  devices: `${BASE_URL}/devices`,
  auth: `${BASE_URL}/auth`,
  me: `${BASE_URL}/users/me`,
};